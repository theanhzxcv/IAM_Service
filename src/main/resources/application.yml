server:
  port: 8081

#keycloak:
#  auth-server-url: http://localhost:8080/auth  # URL của Keycloak Server
#  realm: IAMService  # Tên Realm đã tạo trong Keycloak
#  resource: iam-service  # Client ID được tạo trong Keycloak
#  credentials:
#    secret: t5w40CfqlRxjCVhXRsooZPLPvNmjL27n  # Client Secret được tạo trong Keycloak
#  bearer-only: true  # Cấu hình để Spring Boot nhận diện người dùng và sử dụng Bearer Token
#  use-resource-role-mappings: true  # Nếu muốn sử dụng tài khoản người dùng trong Keycloak để kiểm tra xác thực
#  redirect-uri: /login/*  # Cấu hình các URL chuyển hướng (redirect URIs)
##  realm-public-key: your-public-key  # Kiểm tra thông tin xác thực qua JWT
#  ssl-required: external  # Tăng cường bảo mật cho các yêu cầu khác (tuỳ chọn)

spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/iams
    username: postgres
    password:
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: cb9b9078d8efbf
    password: 472c361eaa061d
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          timeout: 5000

redis:
  host: localhost
  port: 6379

info:
  app:
    name: IAM Service App
    description: IAM Service with Spring Boot, PostgresSQL v� JWT
    version: 1.0.0